- @title = "Edit Project - #{@project.name_was}"

- content_for :header, @project.name_was
= render "admin/tabs"
- content_for :breadcrumbs do
  %ol
    %li= link_to "projects", projects_path
    %li= link_to @project.name_was.downcase, @project
    %li.breadcrumb-muted edit
- content_for :sidebar, render("setup/sidebar")

.dashboard-container
  = render "projects/form", project: @project
