- @title = "#{@viewer.username_was} Edit"

- content_for :sidebar, render("setup/sidebar")
- content_for :header do
  = link_to @viewer.username_was, admin_viewer_path(@viewer)
  Edit

.dashboard-container= render "form", viewer: @viewer
