!!!
%html
  %head
    %meta{ charset: 'utf-8'}
    %meta{ content: 'IE=Edge,chrome=1', http: { equiv: 'X-UA-Compatible' } }
    %meta{ name: 'viewport', content: 'width=device-width, initial-scale=1.0'}
    %meta{ name: 'description', content: 'PATS Trial, a study to evaluate the effects of early adenotonsillectomy (eAT) on the behavior, sleep-disordered breathing symptoms and quality of life for children who snore, but do not have obstructive sleep apnea, as well as identify factors that moderate responses to the surgery.' }
    %meta{ name: 'author', content: '' }
    %meta{ name: 'keywords', content: 'trial, study, child, sleep apnea, tonsils, snore' }

    %title
      = [@title, 'PATS Trial'].compact.join(' - ').html_safe

    = favicon_link_tag
    = csrf_meta_tags

    / HTML5 shim and Respond.js IE8 support of HTML5 elements and media queries
    /[if lt IE 9]
      %script{ src: '//oss.maxcdn.com/libs/html5shiv/3.7.0/html5shiv.js' }
      %script{ src: '//oss.maxcdn.com/libs/respond.js/1.3.0/respond.min.js' }

    = stylesheet_link_tag 'application', media: 'all', data: { turbolinks_track: 'reload' }
    = javascript_include_tag 'application', data: { turbolinks_track: 'reload' }
    = javascript_tag "var root_url='#{request.script_name + '/'}';var auth_token='#{form_authenticity_token}';"
  %body
    = render 'layouts/menu'
    = render 'layouts/flash_messages'

    = yield

    .footer-container.dark-purple
      .container
        = render 'layouts/footer'
