%table.table.table-striped
  %thead
    %tr
      = th_sort_field @order, 'documents.archived', 'Archived', extra_class: 'center'
      = th_sort_field @order, 'documents.document', 'Name'
      %th Size
      = th_sort_field @order, 'documents.created_at', 'Uploaded'
      %th Actions

  - @documents.each do |document|
    %tr
      %td.center
        - if document.archived?
          %span.glyphicon.glyphicon-eye-close.text-muted
      %td= link_to document.name, editor_category_document_path(@category, document), style: "#{'text-decoration: line-through;' if document.archived?}"
      %td
        %code.nowrap= number_to_human_size document.document.size
      %td= document.created_at.strftime('%m-%d-%Y')
      %td
        = link_to 'Edit', edit_editor_category_document_path(@category, document), class: 'btn btn-default btn-xs'
        = link_to 'Delete', editor_category_document_path(@category, document), method: :delete, class: 'btn btn-xs btn-danger', data: { confirm: "Are you sure you want to delete Document #{document.name}?" }

.center
  = paginate @documents, theme: 'bootstrap'
