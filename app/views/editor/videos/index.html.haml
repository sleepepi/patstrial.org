.row
  .col-md-9.order-2
    .section-heading-internal
      %h1
        = link_to 'Folders', editor_categories_path(@category)
        &middot;
        = link_to @category.name, editor_category_path(@category)
        &middot;
        Videos
        = link_to 'Add New Video', new_editor_category_video_path(@category), class: 'btn btn-sm btn-light'
      .section-description
        You may
        %strong archive videos
        you wish to hide from the menu.
    .section-body= render 'videos'
  .col-md-3.order-1
    = render 'setup/menu'
