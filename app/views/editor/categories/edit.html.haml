- @title = "Edit Folder - #{@category.name_was}"

- content_for :header, @category.name_was
= render "admin/tabs"
- content_for :breadcrumbs do
  %ol
    %li= link_to "folders", editor_categories_path
    %li= link_to @category.name_was.downcase, editor_category_path(@category)
    %li.breadcrumb-muted edit
- content_for :sidebar, render("setup/sidebar")

.dashboard-container= render "form", category: @category
